# Backend Requirements
# Flask + Polars + Gunicorn für Railway
# Polars: Schnell, wenig RAM, kein NumPy-Konflikt

Flask==3.0.0
Flask-CORS==4.0.0
polars==1.16.0
gunicorn==21.2.0

# Zusätzliche Dependencies für Production
python-dotenv==1.0.0